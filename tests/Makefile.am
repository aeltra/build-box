# The C tests and the shell tests that drive the C harnesses live in c/,
# the pytest suite for the Python wrapper in python/.  Each has its own
# Makefile so that make check runs both and either can be run alone.
SUBDIRS = c python
